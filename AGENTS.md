## Zig

This project uses Zig 0.16.0.

Before making changes, verify:

    zig version

The expected version is:

    0.16.0

After modifying Zig code, run:

    zig build
    zig build test

Do not use APIs from older Zig versions.
